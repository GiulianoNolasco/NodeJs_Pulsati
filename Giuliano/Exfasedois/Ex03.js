//•Crie um programa que use as novas string templates do ECMAScript 6 para interpolar uma string.

let num1 = 10;
let num2 = 57;
let media = (num1+num2) / 2;
console.log(
  `A média dos números informados (${num1}),(${num2}), é: ${media}`)